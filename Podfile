# Uncomment the next line to define a global platform for your project
platform :ios, '7.0'

target 'iOS-userLocation-smoothMove' do

    pod 'AMap3DMap'

end


target 'iOS-userLocation-smoothMove-Swift' do
    
    pod 'AMap3DMap'
    
end
